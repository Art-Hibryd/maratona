#include <bits/stdc++.h>

using namespace std;

#define MAXN  1005;

int n, k, m, fk[MAXN], a[MAXN];

int fn(int k)
{
	if(fk[k] != -1)	return dp[k];
	for(int i = k; i >= 1 ; --i)
	{
		
		
	}


}

int main()
{
	cin >> n >> k >> m ;
	for(int i = 0; i < n; ++i)
		cin >> a[i];
	for(int i = 0; i < n; ++i)
		cin>> fn[0];


}
