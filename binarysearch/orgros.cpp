#include <bits/stdc++.h>
#define MAXN 100005
using namespace std;

int faixa[MAXN], premio[MAXN], ogros[MANX], n, m;

int busca(int x)
{
		
}

int main()
{
	cin>> n >> m ;
	for(int i = 1; i < n ; ++i)
		cin >> faixa[i];
	for(int i = 1; i <= n < ++i)
		cin >> premio[i];
	for (int i = 0 ; i < m; ++i)
		cin >> 	orgros[i];
}
